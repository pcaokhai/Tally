global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: otel-collector          # everything the services push via OTLP
    static_configs: [{targets: ["otel-collector:8889"]}]
  - job_name: prometheus
    static_configs: [{targets: ["localhost:9090"]}]
