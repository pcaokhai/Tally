# TLY-312 — Webhook signing golden vectors and verification guide

Track A (docs/scripts generator, no service code). Single session, one review pass.

## State found
- AC1 and AC3 already done by prior work: `scripts/gen_webhook_vectors.py` generates
  `contracts/webhooks/signature-vectors.json` (5 vectors matching docs/03 §4) and
  self-verifies them at the end of the script; `make contracts` runs it and then
  `git status --porcelain -- contracts/` fails the build if the checked-in file is stale
  relative to the signing rule. CI's `contracts` job already runs `make contracts`.
- Missing: AC2 — verification snippets for Node/Python/Java/Go in
  `docs/guides/verify-webhooks.md`, checked by a CI job.

## Work
1. `scripts/verify_webhooks/{verify.js,verify.py,Verify.java,verify.go}` — one small
   runnable checker per language. Each reads `contracts/webhooks/signature-vectors.json`,
   recomputes HMAC-SHA256 per docs/03 §4, classifies VALID/REJECT_TIMESTAMP/REJECT_SIGNATURE,
   asserts it matches `expected` for every vector, exits non-zero on mismatch.
2. `docs/guides/verify-webhooks.md` — embeds the same snippets verbatim with short
   run instructions, so the doc never drifts from what CI actually executes.
3. `.github/workflows/ci.yml` — new `webhook-verify-guide` job, gated on the `contracts`
   path filter (vectors or the verify scripts changing), runs all four snippets.

## Verification
- `make contracts` (regenerates + self-checks vectors, fails if stale).
- Run each of the four snippets locally against the generated vectors file, paste output.
